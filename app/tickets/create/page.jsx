import CreateForm from "@/app/components/CreateForm/component";

export default function CreateTicket() {
    return (
        <main>
            <h2 className="text-primary text-center">Add a new ticket</h2>
            <CreateForm />
        </main>
    )
}